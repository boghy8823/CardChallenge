const fonts = {
  primary: "'Helvetica', sans-serif;",
};

const colors = {
  white: "#ffffff",
  blue: "#157AF3",
  navyBlue: "#223C63",
};

const defaultTheme = {
  name: "DEFAULT",
  fonts,
  colors,
};

export default defaultTheme;
